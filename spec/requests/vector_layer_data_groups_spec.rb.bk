require 'rails_helper'

RSpec.describe "VectorLayerDataGroups", type: :request do
  describe "GET /vector_layer_data_groups" do
    it "works! (now write some real specs)" do
      get vector_layer_data_groups_path
      expect(response).to have_http_status(200)
    end
  end
end
